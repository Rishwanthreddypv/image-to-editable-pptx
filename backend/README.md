# Backend API

FastAPI service for processing images and generating PPTX.

## Structure
- `app/api`: Endpoint definitions.
- `app/services`: Core logic (Preprocessing, OCR, Export).
- `app/models`: Database/Internal data models.
- `app/schemas`: Pydantic validation schemas.
